# Updating Dependencies

To update project dependencies, run the following script from the root of the repository:

```
./scripts/update_dependencies.sh
```

This script will handle updating dependencies as needed. Make sure you have the necessary permissions to execute the script.
