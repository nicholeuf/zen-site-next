{
  "extends": "../tsconfig.test.json",
  "compilerOptions": {
    // Keep defaults from tsconfig.test.json — this file exists so editors and
    // Storybook find a nearby tsconfig and associate story files with the test project.
  },
  "include": [
    "../src/**/*",
    "../types/**/*",
    "**/*",
    "../.storybook/**/*"
  ]
}
