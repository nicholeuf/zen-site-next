import React from 'react';
import { Canvas, Meta, Controls, Heading, Subheading } from '@storybook/addon-docs/blocks';
import * as NextLinkStories from './NextLink.stories';

<Meta of={NextLinkStories} />


<Heading>NextLink</Heading>

A thin wrapper around MUI `Link` that uses Next.js `Link` for internal routes defined in the application's `URLS` list.

This component preserves the MUI `Link` API and therefore supports the same props (color, underline, target, sx, etc.).

<Subheading>Example — Playground</Subheading>

<Canvas of={NextLinkStories.Playground} />
<Controls of={NextLinkStories.Playground} />

<Subheading>Interaction examples</Subheading>

<Subheading>Focus (keyboard)</Subheading>

Use the `FocusVisible` story in the story list to see keyboard focus styles. The original story automatically tabs to the link using a play function.

<Canvas of={NextLinkStories.FocusVisible} />
<Controls of={NextLinkStories.FocusVisible} />

<Subheading>Hover state</Subheading>

Use the `Hover` story (pseudo state) to preview hover styles in isolation.

<Canvas of={NextLinkStories.Hover} />
<Controls of={NextLinkStories.Hover} />
